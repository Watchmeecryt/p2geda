OWNER_PRIVATE_KEY=0x
RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
VAULT_ADDRESS=0x06742409F042B3c5932c6C154B9CE67929076eD0
CHAIN_ID=11155111
# Hourly rounds: poll every 2 minutes is enough (default in code is 120000).
KEEPER_POLL_INTERVAL_MS=120000

# Indexer only (optional if you only run the keeper)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Used only by `npm run backfill` — normal `start:indexer` stays at the chain tip.
DEPLOYMENT_BLOCK=11625426
LOG_CHUNK=10
POLL_INTERVAL_MS=15000
CONFIRMATIONS=3
